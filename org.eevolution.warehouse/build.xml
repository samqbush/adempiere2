<project name="WarehouseManagement" default="zkdist" basedir=".">
    <description>
        Building Libero liberoWM.jar
        by
        Victor Perez 
        http://www.e-evolution.com
    </description>
  <!-- set global properties for this build -->
  <property file="build.properties"/>
  <import file="${adempiere.trunk}/utils_dev/properties.xml"/>
  <import file="${adempiere.trunk}/utils_dev/phase5d-frozen-zk.xml"/>
  <property name="src" location="src/main/java/base"/>
  <property name="build" location="build"/>
  <property name="dist"  location="dist"/>
  <property name="zksrc" value="${basedir}/src/main/java/ui/zk"/>
  <property name="zkclasses" value="${basedir}/build/zkwebui/WEB-INF/classes"/>

  <path id="lib.class.path">
     <fileset dir="${adempiere.trunk}/lib">
        <include name="*.jar"/>
     </fileset>
    <fileset dir="${adempiere.trunk}/packages">
       <include name="*.jar"/>
    </fileset>
  </path>
  <path id="compile.classpath">
    <pathelement location="${basedir}/build"/>
    <pathelement location="${adempiere.trunk}/zkwebui/WEB-INF/classes"/>
    <fileset dir="${adempiere.trunk}/lib">
        <include name="*.jar"/>
    </fileset>
    <fileset dir="${adempiere.trunk}/zkwebui/WEB-INF/lib">
        <include name="*.jar"/>
    </fileset>
  	<fileset dir="${adempiere.trunk}/org.eevolution.distribution/dist/lib">
  	  <include name="*.jar"/>
  	</fileset>
  </path>

  <target name="init" depends="clean">
    <echo message="Trunk location: ${adempiere.trunk}"/>
    <!-- Create the time stamp -->
    <tstamp/>
    <!-- Create the build directory structure used by compile -->
    <mkdir dir="${build}"/>
    <!-- Create the classes directory structure used by compile -->
    <mkdir dir="${zkclasses}"/>
  </target>

  <target name="compile" depends="init"
        description="compile the source " >
    <!-- Compile the java code from ${src} into ${build} -->
    <javac encoding="UTF-8" srcdir="${src}" destdir="${build}" includeantruntime="false">
      <exclude name="**/org/eevolution/plugin/*.java"/>
      <classpath refid="lib.class.path"/>
    </javac>

  </target>

  <target name="dist" depends="compile"
        description="generate the distribution" >
    <!-- Create the distribution directory -->
    <mkdir dir="${dist}/lib"/>

    <!-- Put everything in ${build} into the MyProject-${DSTAMP}.jar file -->
    <jar jarfile="${dist}/lib/WarehouseManagement.jar" basedir="${build}"/>

    <!-- Create target Libero deployment -->
    <mkdir dir="${env.ADEMPIERE_HOME}/packages/liberoWM/lib"/>

    <!-- Transfer liberoWM.jar -->
    <copy file="${dist}/lib/WarehouseManagement.jar" todir="${env.ADEMPIERE_HOME}/packages/WarehouseManagement/lib"/>
    <copy file="${dist}/lib/WarehouseManagement.jar" todir="${adempiere.trunk}/packages/"/>

  </target>

    <target name="zkcompile" depends="dist, phase5d.materialize-frozen-zk"
            description="stage the frozen legacy ZK package jar" >
        <!-- Phase 5d: src/main/java/ui/zk now targets ZK CE 10.3.0.1-jakarta and is
             compiled by Gradle (:org.eevolution.warehouse:compileZkUiJava). Compiling it here
             against the ZK 3.6 jars in ../zkwebui/WEB-INF/lib is impossible, and
             compiling it against ZK CE would change the shipped legacy artifact the
             Phase 5b oracle is pinned to, so the frozen jar built from the Phase 5b
             source commit is staged instead. -->
        <echo message="Phase 5d: staging the frozen WarehouseManagementZk.jar"/>
    </target>

    <target name="zkdist" depends="zkcompile"
            description="generate the distribution" >
        <!-- Create the distribution directory -->
        <mkdir dir="${dist}/lib"/>
        <!-- Phase 5d: stage the frozen legacy ZK package jar instead of rebuilding it -->
        <copy file="${phase5d.frozen.zkpackages}/WarehouseManagementZk.jar" tofile="${dist}/lib/WarehouseManagementZk.jar" overwrite="true"/>

        <!-- Create target Liberozk deployment -->
        <mkdir dir="${env.ADEMPIERE_HOME}/zkpackages/WarehouseManagement/lib"/>

        <!-- Transfer liberozkWM.jar -->
        <copy file="${dist}/lib/WarehouseManagementZk.jar" todir="${env.ADEMPIERE_HOME}/zkpackages/WarehouseManagement/lib"/>
        <copy file="${dist}/lib/WarehouseManagementZk.jar" todir="${adempiere.trunk}/zkpackages/"/>
    </target>


  <target name="clean"
        description="clean up" >
    <!-- Delete the ${build} and ${dist} directory trees -->
    <delete dir="${build}"/>
    <delete dir="${dist}"/>
    <delete dir="${zkclasses}"/>
  </target>
</project>
